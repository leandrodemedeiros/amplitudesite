---
import { getCollection } from "astro:content";
import { Picture } from "astro:assets";
import uxdesign from "assets/puzzle.png";
import auto from "assets/connections.png";
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";

// Filter team entries with 'draft: false' & date before current date
const publishedTeamMembers = await getCollection("team", ({ data }) => {
  return !data.draft && data.publishDate < new Date();
});
---

<Layout title="About">
  <Container>
    <Sectionhead>
      <Fragment slot="title">O que fazemos</Fragment>
      <Fragment slot="desc">(am·pli·tu·de)</Fragment>
    </Sectionhead>

    <!-- <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-gray-800">
        Onde tudo começa: Identificar estratégias
      </h2>
      <p class="text-lg leading-relaxed text-slate-500">
        A qualificação do seu público vai ajudar a identificar como podemos atacar melhor, nos pontos fortes e fracos. 
      </p>
      
      <h2 class="font-bold text-3xl text-gray-800">Vamos identificar estratégias em pontos chave e orquestrar ações específicas.</h2>
    </div> -->
    <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
      <Picture
          src={uxdesign}
          alt="Astronaut in the air"
          widths={[200, 400, 600]}
          sizes="(max-width: 800px) 100vw, 620px"
          loading="eager"
          format="avif"
          id="uxdesign"
        />
        <Picture
          src={auto}
          alt="Astronaut in the air"
          widths={[200, 400, 600]}
          sizes="(max-width: 800px) 100vw, 620px"
          loading="eager"
          format="avif"
          id="uxdesign"
        />
        <Picture
          src={uxdesign}
          alt="Astronaut in the air"
          widths={[200, 400, 600]}
          sizes="(max-width: 800px) 100vw, 620px"
          loading="eager"
          format="avif"
          id="uxdesign"
        />
        <!-- <img src="" { 
        //publishedTeamMembers.map((teamMemberEntry) => (
          // <div class="group">
          //   <div class="w-full aspect-square">
          //     <Picture
              //   src={teamMemberEntry.data.avatar.src}
              //   alt={teamMemberEntry.data.avatar.alt}
              //   sizes="(max-width: 800px) 100vw, 400px"
              //   width={400}
              //   height={400}
              //   class="w-full rounded-md rounded transition group-hover:-translate-y-1 group-hover:shadow-xl bg-white object-cover object-center aspect-square"
              // />
            // </div>

      //       <div class="mt-4 text-center">
      //         <h2 class="text-lg text-gray-800">{teamMemberEntry.data.name}</h2>
      //         <h3 class="text-sm text-slate-500">
      //           {teamMemberEntry.data.title}
      //         </h3>
      //       </div>
      //     </div>
      //   ))
      // } -->
    </div>
  </Container>
</Layout>
